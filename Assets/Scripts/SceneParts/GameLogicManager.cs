using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using TMPro;

public class GameLogicManager : MonoBehaviour
{
	public ScenePlayerManager playerManager;
	public TextMeshProUGUI eventFeed;
	
	// game states:
	// waiting for players
	// counting down start
	// playing game
	// game finished
	// resetting game
	
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
